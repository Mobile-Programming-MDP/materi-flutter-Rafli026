name: pelaporankejadian_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8

  # Images
  image_picker: ^1.1.2
  flutter_image_compress: ^2.3.0

  # Location
  geolocator: ^12.0.0

  # Map
  flutter_map: ^7.0.2
  latlong2: ^0.9.1

  # Share
  share_plus: ^10.0.2
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0
  path: ^1.9.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
