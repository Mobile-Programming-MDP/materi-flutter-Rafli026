# pilem

A simple movie app demonstrating:

- Consumption of the RESTful TMDB API (`ApiService`).
- Search, home, detail and favorite screens.
- Favorite movie persistence using `shared_preferences` (see `FavoriteService`).

Run `flutter pub get` after cloning, then `flutter run` on your selected device/emulator.
